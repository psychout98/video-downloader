# Base URL for Playwright E2E tests
# Set this to your Windows VM's IP when running against the full stack
# Find the VM's IP by running `ipconfig` inside the VM
E2E_BASE_URL=http://192.168.1.42:5173
